using UnityEngine;

public class Arrays : MonoBehaviour
{
                         // 0 1  2  3  4
    public int[] numeros = {8,54,32,7,78};

    //public string nombre;
    public string nombre = "Profe es muy guapo";

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

        //for (int i = 0; i < nombre.Length; i++)
        //{
        //    if (nombre[i] == 'a')
        //    {
        //        print("El nombre dado contiene la letra A");
        //        break;
        //    }
        //}


        //numeros = new int[] { 8, 54, 32, 7, 78 };
        ////print(numeros[3]);


        //for (int i = 0; i < numeros.Length; i++)
        //{
        //    if(numeros[i] == 32)
        //    {
        //        print("El numero " + 32 +  "se encuentra en la posicion:" + (i + 1));
        //        break;
        //    }
        //    print("En el indice: " + i + "Esta el valor: " + numeros[i]);
        //}




    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
